# Web dashboard
flask>=3.0.0
flask-wtf>=1.2.0
gunicorn>=22.0.0

# Scraping
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
pyyaml>=6.0.0
playwright>=1.40.0          # JS-rendered PM sites; ALSO run: playwright install chromium

# AI enrichment / scoring
anthropic>=0.86.0           # needs messages.parse(output_format=...) structured outputs
pydantic>=2.0               # enrichment schema (also a transitive anthropic dep)
python-dotenv>=1.0.0

# Tests
pytest>=8.0.0

# ---------------------------------------------------------------------------
# After `pip install -r requirements.txt`, install the Playwright browser:
#     python -m playwright install chromium
# (one-time; downloads the headless Chromium binary the JS scrapers use)
# ---------------------------------------------------------------------------
